export interface UserPreferences {
  userId: string;
  email: string;
  mode: string;
  zipperEnabled: boolean;
  spaceChar: string;
  skipOverTabs: boolean;
  cursorChar: string;
  smoothCursor: boolean;
}
